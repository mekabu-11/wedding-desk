<%= link_to "← 世帯", guests_path(tab: "households"), class: "back-link" %><header class="page-heading"><div><h1>世帯を編集する</h1><p><%= @household.guests.size %>人のゲストが所属しています。</p></div></header><section class="surface narrow"><%= render "form", household: @household, cash_gift_rules: @cash_gift_rules %><%= button_to "この世帯を削除", household_path(@household), method: :delete, class: "button danger-button", style: "margin-top: 16px", form: { data: { turbo_confirm: "削除しますか？" } } %></section>
