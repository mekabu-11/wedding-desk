<%= link_to edit_task_path(task), class: "task-row" do %><% unless local_assigns.fetch(:hide_status_check, false) %><span class="task-check <%= 'done' if task.status == 'done' %>"><%= task.status == 'done' ? '✓' : '' %></span><% end %><div class="row-content"><h3><%= task.title %></h3><div class="micro"><span class="<%= 'overdue' if task.overdue? %>"><%= deadline_label(task) %><%= ' · 期限超過' if task.overdue? %></span><span class="separator">/</span><%= task.assignee_label %></div></div><span class="badge <%= task.status == 'doing' ? 'amber' : 'muted' %>"><%= Task::STATUSES[task.status] %></span><span class="row-arrow">→</span><% end %>
