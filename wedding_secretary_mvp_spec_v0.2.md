# 結婚式の連絡整理AI秘書 MVP仕様書

- 文書版：v0.2（設計レビュー反映版）
- 作成日：2026-09-06
- 対象：開発者本人が実際の結婚式準備で使う、非公開Webアプリ
- この文書は設計案。アプリの実装・公開を開始する指示とは分けて扱う。

## 1. プロダクト概要

メール・LINE・PDF・画像・打ち合わせメモに分散した結婚式の情報を登録すると、AIが要約し、タスク・決定事項・回答待ち・見積変更の候補を整理する。

コンセプトは「結婚式準備の連絡を、探し直さなくていい」。原文を完全に見なくてよくなることや、AIがすべて正確に確定することは約束しない。必要なときは、整理された項目から根拠の原文へすぐ戻れる。

AIは情報の抽出と整理を担当し、金額・期限・確定状態など重要な情報は利用者が確認する。外部への送信、契約、支払いは行わない。

## 2. MVPの対象と仮説

対象は、挙式まで1〜6か月程度で、式場とのやり取りが始まっており、メール・LINE・PDFに情報が分散している人。

最初は開発者本人の1アカウント・1結婚式で検証する。パートナーの担当は記録できるが、別アカウントによる夫婦共有は次段階とする。アカウントの共有は前提にしない。本人利用の結果だけで夫婦共同利用の有効性を検証済みとは扱わない。

中心仮説：資料の登録と抽出結果の確認にかかる手間を含めても、連絡を探し直して状況を整理する時間を減らせる。

検証する問い：

- 今やること・回答待ち・見積の変更を30秒以内に見つけられるか。
- 新しい連絡の登録と確認を、日常的に続けられるか。
- 元資料を探し直す回数と作業時間が減るか。
- 重要な金額・期限の誤りや見落としを検出できるか。

## 3. MVPの範囲

必須機能：

- ログイン、結婚式の基本設定
- テキスト貼り付け、PDF・JPG・PNGの登録
- 非同期解析、失敗表示、再試行
- 原文と根拠付きの抽出候補表示
- 候補の確認・編集・却下、手動項目追加
- タスク、決定事項、回答待ちの管理
- 見積書の版管理と比較、現在採用する見積の選択
- ダッシュボード、情報一覧、原文へのアクセス
- 原資料・派生情報の削除、アクセス制御

対象外：

- Gmail・LINE直接連携、メール転送取り込み
- 自動メール送信、契約・決済の実行
- カレンダー連携、プッシュ通知、外部への期限通知
- AIチャット、ネイティブアプリ
- 招待状・席次表・ご祝儀管理、業者検索
- 夫婦の別アカウント共有、複数結婚式、料金プラン
- 請求と支払いの照合、未払残高の会計管理
- あらゆる形式の見積書の完全自動比較

通知はMVPにないため、期限は画面上で確認する運用とする。

## 4. 結婚式の基本情報

- 結婚式名
- 挙式日（未定の場合は空欄可）
- 会場名
- 本人の表示名、パートナーの表示名
- 当初予算（JPY・整数円、未設定可）
- タイムゾーン（初期値Asia/Tokyo）

担当は `self / partner / both / unknown` とする。資料に担当の明記がなければ `unknown`。人物名と担当の対応は利用者が設定し、性別・作業内容から推定しない。

現在の見積額は結婚式レコードへの自由入力値ではなく、採用中の見積から集計する。挙式までの日数はプロジェクトの現地日付で計算し、挙式当日・終了後・日付未設定を区別する。

## 5. 情報登録

### 5.1 テキスト

入力項目：タイトル、本文、情報元、送受信方向、発信者・宛先（任意）、連絡日時、カテゴリ（任意）。

- 情報元：`email / line / meeting / other`
- 方向：`incoming / outgoing / memo / mixed / unknown`
- 登録日時と連絡日時を分ける。連絡日時が不明なら空欄とし、登録日で代用しない。
- タイトルは省略可。本文から仮タイトルを生成できる。
- 複数メッセージを貼った場合は、読み取れる範囲でメッセージごとの発信者・日時・本文位置を保持する。不明な部分は推測で埋めない。
- 口頭メモも根拠資料として扱うが、発言内容から確定・提案・質問を区別する。

### 5.2 ファイル

PDF・JPG・PNGを受け付ける。情報元とファイル形式は別項目とし、「メールに添付されたPDF」を表現できるようにする。

MVPの初期制限は、1登録につき1ファイル、20MB以内、PDFは20ページ以内。テキストは1登録30,000文字以内。実資料で必要なら見直す。

- テキストPDFは文字抽出、スキャンPDF・画像はOCRまたは画像対応解析を使う。
- 暗号化PDF・破損・形式不一致・上限超過は理由を表示し、再登録を案内する。
- 上限超過や読めないページを黙って切り捨てない。部分抽出はその範囲を明示する。
- 小さい文字・表の崩れ・欠けたページがある場合は、確認が必要な箇所を表示する。
- 同じ原文または同じファイルの完全一致はハッシュで検知し、既存資料を案内する。似た文章は重複候補に留め、自動削除しない。
- 原文の修正は新しい版として登録する。元の版と抽出履歴を残す。

## 6. 原文・候補・確定情報を分ける

処理は次の順で行う。

```text
資料登録 → 文字抽出 → AI解析 → 構造・値・根拠の検証
→ 抽出候補を保存 → 原文と並べて確認 → 確定情報へ反映
```

要約とカテゴリは「AIによる整理」と表示し、自動表示できる。タスク・期限・担当・決定事項・回答待ち・見積・変更は候補として保存し、確認前に確定欄や正式な金額集計へ入れない。

原文が明確な候補はまとめて確認できる。曖昧な期限、読めない金額、既存情報と矛盾する候補は個別確認を求める。確認済み項目を新資料や再解析で自動上書きしない。

各候補には以下を持つ。

```text
kind, payload, review_status
document_id, analysis_run_id
evidence[]: 原文引用、ページ番号または本文位置
uncertainty_reasons[]
proposed_target_id（既存項目の更新候補の場合）
```

`review_status = pending / accepted / rejected / superseded`。

AIが返す信頼度の数値だけで確定しない。JSON Schemaへの適合は構造の検証であり、内容の正しさの証明ではない。引用位置・引用文の整合も検証し、根拠不明な候補はその旨を表示する。

## 7. 抽出する情報

### 7.1 要約とカテゴリ

カテゴリは元案の `venue / guest / food / drink / dress / tuxedo / flower / photo / movie / music / gift / invitation / accommodation / insurance / payment / schedule / other` を維持する。

1資料には複数カテゴリを付けられる。個別項目にもカテゴリを付ける。要約では、決定と提案・質問・予定を混同しない。

### 7.2 決定事項

`title, description, category, decided_at, status, evidence` を保持する。

- `status = active / superseded / cancelled`
- 「レンタル予定」「検討中」「いかがでしょうか」は確定事項にしない。
- 決定日が不明なら空欄。登録日時を決定日として表示しない。
- 新しい決定で置き換える際も以前の内容と根拠を履歴として残す。

### 7.3 タスクと期限

`title, description, assignee, due_on, due_at, original_due_text, date_basis, status, category, evidence` を保持する。

- `status = todo / doing / done / cancelled`
- 日付だけの期限は `due_on`、時刻まで明示されるものはタイムゾーン付きの `due_at`。無断で時刻を補完しない。
- 「明日」は該当メッセージの日付を基準にする。日時不明・「来週中」・年省略は、解釈候補と理由を示して確認する。
- AIが補完した年・日付は未確認のまま確定しない。
- 期限不明は空欄とし、「期限未設定」に表示する。
- 同じ依頼の引用・再掲は新タスクにせず、既存項目への関連付け候補とする。
- 完了を示す連絡も更新候補とし、承認前に自動完了しない。
- 期限は原則タスクに紐付ける。期限だけ見つかった場合は「対象の確認が必要な候補」にし、独立した期限表と二重管理しない。

### 7.4 回答待ち

`title, counterparty, requested_at, status, resolved_at, evidence` を保持する。

- `status = waiting / resolved / cancelled`
- 自分から送信済みの質問・依頼で、回答が必要なものを候補にする。
- 相手からの「ご返信ください」は自分のタスク。送信前の下書きは回答待ちにしない。
- 履歴が部分的なら、既に返信が来ているか判断できないため確認を求める。
- 回答の到着は、対応する質問と根拠を示した解決候補にする。
- 質問日不明なら経過日数を表示しない。

### 7.5 人物

人物名は担当・発信者・相手先の補助情報として抽出する。MVPでは独立した連絡先管理や住所録は作らない。資料に不要な住所・電話番号を構造化して蓄積しない。

## 8. 金額と見積の管理

元案の `estimate / confirmed / paid / additional` は、資料の種類・確定状態・支払い・増分を混ぜているため使用しない。

### 8.1 金額の抽出と総額への採用は別

メール中の追加費用、請求書、支払い連絡は「金額情報」として抽出するが、見積総額へ自動加算しない。

例：新見積5,020,000円に装花追加80,000円が含まれている場合、合計を5,100,000円にしない。請求額や既払い額も見積に足さない。

金額情報には、金額、通貨、用途、`estimate / quote / invoice / payment / other`、税込・税別・不明、根拠を保持する。通貨はMVPではJPYを集計対象とし、他通貨は自動換算しない。欠損は0円にしない。

### 8.2 見積グループと版

同じ業者の同じ契約範囲を `EstimateGroup` として管理する。例：「式場一式」「外注映像」。同じ業者でも別契約は別グループにできる。

各見積には以下を保持する。

- 業者名、見積番号（あれば）、発行日、対象範囲
- 原資料、前版の候補、利用者が確認した比較対象
- 記載総額、通貨、税区分
- 明細、値引き、税・サービス料、計算照合状態
- 確認状態と、そのグループで現在採用する版かどうか

1グループにつき採用する版は最大1つ。アップロード日時では採用版を決めない。古い見積を後日登録しても現在額は変わらない。

明細は原文の項目名、数量、単位、単価、記載金額、税・サービス料に関する記載、ページ・行位置を保持する。数量は小数を扱える形、JPY金額は整数で扱う。記載値と計算値を分け、無断で補正しない。

### 8.3 ダッシュボードの集計

「登録済み範囲の見積合計」＝利用者が採用し、集計対象にした各グループの見積総額の合計。

- 式場一式に含まれる宿泊を別グループでも登録した場合は、内包関係を確認して集計から除外する。
- 集計対象・対象外・未確認を利用者が確認する。重複範囲をAIが完全に判定できる前提にしない。
- 未登録・未確認の費用がある可能性を表示し、「結婚式の最終総費用」と断定しない。
- 予算との差は計算するが、見積合計から支払い済み金額を引かない。
- 採用見積がなければ「未登録」。0円と表示しない。

## 9. 変更検知

同一カテゴリというだけでは同一項目とみなさない。業者・契約範囲・項目名・既存の関連付けを使って比較候補を作る。

### 9.1 見積比較

1. 比較する前後の見積を確認する。
2. 税区分・対象範囲が比較可能か確認する。
3. 明細の対応候補を作り、不明な対応は利用者が指定する。
4. 金額差・数量差・単価差はコードで計算する。
5. 総額差と説明できる内訳差を照合する。

表示するもの：前回総額、今回総額、総額差、対応済み明細差、新規・削除候補、対応不明の明細、未説明差額。

部分的な資料や抽出失敗で項目が見えない場合は、削除と断定しない。税・サービス料・値引きで内訳差と総額差が一致しない場合も、差を隠さない。

例：4,800,000円 → 5,020,000円は +220,000円。装花+80,000円、衣装+100,000円、宿泊+40,000円なら差額が一致する。内訳不明なら総額差だけ示し、内訳を創作しない。

### 9.2 タスク・決定・回答待ちの変更

新しい期限、決定の変更、質問への回答は、既存項目への更新候補として提示する。利用者の確認後に反映し、旧値・新値・両方の根拠・反映者・反映日時を保存する。

登録日時と実際の連絡日時を区別する。時系列不明または矛盾がある場合は、自動で「最新」を決めない。

## 10. 画面

### ダッシュボード

表示順：

1. 未確認の情報：重要な期限・金額・矛盾と解析失敗件数
2. 今やること：期限超過、期限が近い順、期限未設定
3. 回答待ち：相手先、質問内容、経過日数
4. 最近決まったこと：確認済みの決定のみ
5. お金：登録済み範囲の見積合計、予算差、集計範囲、未確認件数
6. 最近の変更：確認済みの差分と原文リンク

未確認の期限候補を通常タスクに混ぜない一方、存在が埋もれない位置に表示する。挙式までの日数は動的に計算する。

### その他の画面

- 登録・ログイン
- 結婚式設定
- 情報登録（テキスト／ファイル）
- 情報一覧：情報元、カテゴリ、連絡日、解析・確認状態で絞り込み
- 情報詳細：原文／PDFの該当ページ、要約、根拠付き候補、確認操作
- タスク・決定事項・回答待ちの一覧と編集
- 見積一覧・比較・採用版の切り替え
- データ削除・利用設定

白背景を中心に、日付・金額・状態・根拠を読み取りやすくする。チャット中心にせず、確認操作をできるだけまとめる。「整理された結果」に加え、未確認・解析失敗・比較不能も明示する。

## 11. 修正・再解析・削除

- 利用者の手修正をAIの再解析で上書きしない。
- 再解析は新しい `AnalysisRun` と候補群を作る。過去の却下・採用結果を保持し、同じ候補を黙って再登録しない。
- 二重実行や再送でも確定項目・変更イベントが重複しないよう、処理単位と候補反映に一意性を設ける。
- 同じ候補の二重承認や、古い画面からの上書きを防ぐ。
- 誤った候補は却下。採用済み項目は編集・取消ができ、変更履歴を残す。
- 原資料削除の前に、その資料を根拠とする項目と見積への影響を表示する。
- 原資料削除ではファイル・抽出本文・引用・解析候補を削除する。確認済みの派生情報を残す場合は、利用者が明示的に手動情報として残すことを選ぶ。選ばないものは削除または採用解除する。
- 削除済み資料を処理中のジョブが再作成しない。削除直後から画面・APIアクセスを拒否する。
- プロジェクト削除では原資料・派生情報を一括削除する。バックアップの保持期間は運用開始前に定めて表示する。

## 12. AI処理と障害対応

```text
Document登録
→ AnalysisRun作成
→ 永続ジョブへ登録
→ テキスト抽出／OCR
→ LLM解析
→ JSON Schema・値・原文参照の検証
→ 候補一式をトランザクションで保存
→ review_pending
→ 利用者確認
→ 確定情報・変更履歴へ反映
```

解析状態：`pending / processing / review_pending / completed / partial / failed`。

- `review_pending` は解析成功・未確認候補あり、`completed` は候補の確認完了。候補0件も正常結果になり得る。
- `partial` は読めない範囲がある状態。候補を利用できても、不完全表示を維持する。
- 一時エラーは回数上限付きで再試行する。形式不備などは自動再試行せず、理由を表示する。
- 処理が止まった場合はタイムアウト検知し、再試行できるようにする。
- 原資料は失敗しても保持する。失敗を「抽出項目なし」と表示しない。
- モデル、プロンプト、スキーマの版、処理時間、利用量、エラー種別を記録する。
- モデル名・API鍵は環境変数で設定する。鍵をブラウザへ渡さない。
- 文書中の「以前の指示を無視」等は資料の内容として扱う。解析には外部操作権限を与えない。
- AIへの入力は対象資料と必要な比較資料に限定し、別プロジェクトの情報を混ぜない。

AIは文章理解・分類・抽出・関連付け候補を担当する。計算・認可・採用版管理・期限比較・確定更新・完全一致重複判定はコードで処理する。意味的な重複は候補として利用者が判断する。

## 13. データモデル案

全ての業務データをWeddingに紐付け、別Weddingの資料や項目を参照できない制約・認可を設ける。以下は主要フィールドで、通常の作成・更新日時は省略する。

| テーブル | 主なフィールドと役割 |
| --- | --- |
| users | email, name, 認証情報 |
| weddings | user_id, name, wedding_date, venue_name, self_name, partner_name, budget_yen, timezone |
| documents | wedding_id, title, source_type, direction, sender, recipient, occurred_at, original_text, content_hash, supersedes_document_id, analysis_status |
| document_files | document_id, 非公開ストレージ参照, mime_type, byte_size, checksum, page_count |
| document_segments | document_id, page_number, text, position_data：抽出本文と根拠位置 |
| analysis_runs | document_id, status, model_version, prompt_version, schema_version, attempt, error_code, started_at, finished_at |
| summaries | document_id, analysis_run_id, content, categories |
| extraction_candidates | wedding_id, analysis_run_id, kind, payload_json, review_status, uncertainty_reasons, proposed_target_id, applied_entity_type, applied_entity_id |
| evidence_links | wedding_id, document_id, segment_id, candidate_idまたは確定項目参照, quote, locator |
| tasks | wedding_id, title, description, assignee, due_on, due_at, original_due_text, date_basis, status, category, lock_version |
| decisions | wedding_id, title, description, category, decided_at, status, lock_version |
| waiting_items | wedding_id, title, counterparty, requested_at, status, resolved_at, lock_version |
| monetary_mentions | wedding_id, amount_yen, currency, purpose, kind, tax_basis：請求・支払い等を含む金額情報。総額集計には直接使わない |
| estimate_groups | wedding_id, vendor_name, scope, included_in_group_id, inclusion_status, current_estimate_id |
| estimates | wedding_id, estimate_group_id, document_id, estimate_number, issued_on, stated_total_yen, currency, tax_basis, review_status, reconciliation_status |
| estimate_lines | estimate_id, source_label, quantity, unit, unit_price, stated_amount_yen, component_type, evidence参照 |
| estimate_line_matches | previous_line_id, current_line_id, review_status：同一グループ内の版間対応 |
| change_events | wedding_id, entity_type, entity_id, field_name, old_value, new_value, old_evidence, new_evidence, actor_id, applied_at |

確定項目は複数資料を根拠にできる。単一の `document_id` だけで由来を表現しない。候補JSONと確定テーブルを分け、LLM出力をそのまま確定テーブルへ保存しない。

## 14. 技術構成

元案どおり、Railsのモノリスを採用する。

- Ruby on Rails、PostgreSQL
- Railsビュー、Hotwire／Turbo／Stimulus、Tailwind CSS
- Active Storageと非公開ストレージ
- Active Jobと永続キュー、独立したワーカープロセス
- PDF文字抽出・OCR／画像解析
- JSON Schemaに沿った構造化出力を利用できるLLM API

Reactは初期導入しない。Webを再起動しても解析ジョブが失われない構成にする。Rails・Ruby・ジョブ基盤・認証方式・LLM提供元・ホスティングの具体的なバージョンと設定は実装計画で検証して確定する。

## 15. セキュリティとデータ取り扱い

- 非公開の本人利用でもログイン必須。自己登録は初期段階では無効にする。
- 画面・API・ファイル取得の全経路でWeddingの所有権を検証する。
- ストレージの非公開設定だけで完了とせず、ファイル取得時にもアプリで認可する。未認証で使える恒久URLを配布しない。
- HTTPS、保存時暗号化、秘密情報の環境変数管理を行う。
- 原文・氏名・連絡先・ファイル内容・LLMの生入出力をログやエラー監視へ送らない。
- ファイル形式・サイズ・ページ数を検証し、処理時間とメモリ使用量を制限する。抽出内容をHTMLとして実行しない。
- AI提供元へ送る資料の範囲、利用目的、保存・学習利用の設定を実装時に確認して説明する。提供元未定の段階で「保存されない」と断定しない。
- 不要な住所・招待者名簿等は取り込み前に除く運用を案内する。
- ストレージ・DB・バックアップの削除方針と保持期間を運用開始前に定める。

## 16. 実装順序

### Phase 1：テキストの最小フロー

ログイン → Wedding設定 → テキスト登録 → 非同期解析 → 原文付き候補確認 → タスク反映 → 一覧表示。再試行・二重反映防止・所有権検証まで含める。

### Phase 2：実際の見積PDFで主要価値を検証

最初から本人の見積書2版を使い、PDF登録・抽出・総額確認・比較・採用版を完成させる。PDFと差分比較を最後まで後回しにしない。画像・スキャン資料も少なくとも1件検証する。

### Phase 3：情報管理を拡張

決定事項、回答待ち、複数資料の関連付け、変更候補、手修正保護、取消・削除。

### Phase 4：ダッシュボードと実運用

期限・回答待ち・採用見積・未確認候補をまとめる。実際の準備で継続利用し、登録・確認の手間と探し直し時間を測る。

本人用MVPの完了は公開サービスとしての完成を意味しない。夫婦共有や外部ユーザー募集は、権限・運用・利用条件を整備してから別途判断する。

## 17. 受け入れ条件

| ケース | 期待する結果 |
| --- | --- |
| 担当と期限が明記された依頼メール | 根拠付き候補を作り、確認後にタスク化する |
| 担当が書かれていないメール | 性別や内容から担当を決めずunknownにする |
| 年省略・相対日付、連絡日時不明 | 解釈の根拠と要確認を表示し、無断確定しない |
| 「母親衣装をレンタル予定」 | 確定済みの決定として表示しない |
| 相手から返信を求めるメール | 自分のタスク候補にし、相手の回答待ちにしない |
| 自分の送信済み質問と、その後の回答 | 回答待ち候補と解決候補を対応付ける |
| 5,020,000円の見積PDF | 記載総額とページ根拠を表示し、確認後に採用できる |
| 4,800,000円と5,020,000円の2版 | 総額差+220,000円をコードで算出する |
| 総額差と明細差が一致しない | 未説明差額を表示し、内訳を補造しない |
| 追加80,000円のメールと、それを含む新見積 | 追加額を見積へ再加算しない |
| 見積・請求・支払いが同額 | 3倍の費用として集計しない |
| 古い見積を後日登録 | 採用中の見積を自動で置き換えない |
| PDFの一部が読めない | 不完全と表示し、見えない項目を削除扱いしない |
| 同じ資料の再登録・ジョブ再試行・承認二重送信 | 確定項目や変更イベントを重複生成しない |
| 手修正後に再解析 | 手修正を維持し、変更は候補として出す |
| AI障害・抽出失敗 | 原資料が残り、失敗理由と再試行を表示する |
| 別ユーザーの資料ID・ファイル参照 | 原文・要約・派生情報へアクセスできない |
| 資料削除中に解析が完了 | 削除済み内容を復活させない |
| ダッシュボード | 今やること・回答待ち・見積変化・未確認情報を30秒以内に見つけられる |

テスト資料は匿名化した実資料を中心に、メール・LINE・メモ・通常PDF・スキャン／画像を含める。最低20資料と見積2版以上を人手で正解付けし、重要な期限・金額の抽出漏れ、誤抽出、確認負担を測る。資料数は初期の検証規模であり、品質保証の統計的根拠ではない。

## 18. 成功判断

受け入れ条件を満たすことは機能完成の条件。需要や実用性の証明とは分ける。

本人が2週間、対象となる新しい連絡を登録して使い、次を記録する。

- 登録から確認完了までの作業時間
- 原資料を探し直した回数と所要時間
- 抽出候補の採用・修正・却下件数、抽出漏れ
- 金額・期限の重大な誤り
- 登録を見送った資料と理由

「30秒で状況が分かる」に加え、確認作業を含む総作業時間が従来より減ることを継続開発の判断材料とする。確認が面倒で使わなくなる場合は、自動化機能を増やす前に確認画面と入力フローを改善する。

## 19. 次段階

本人利用で価値を確認した後、夫婦共有、メール転送、Gmail等の連携、期限通知、質問候補へ拡張する。

夫婦共有では招待・メンバー権限・操作履歴・同時編集を追加する。担当は本人の設定と明示された依頼を優先し、作業内容から自動的に性別へ割り当てない。

## 20. 元案からの主な修正

1. AI候補と確認済み情報を分離し、根拠表示を必須化。
2. 金額一覧の単純合算を廃止し、見積グループ・版・採用範囲を導入。
3. 請求・支払い・追加額の二重計上を防ぐ仕様を追加。
4. アップロード順で最新情報を決めず、連絡日・発行日・確認操作を区別。
5. 担当・相対日付・予定と決定・回答待ちの判断規則を明確化。
6. 再解析・重複実行・手修正保護・削除時の派生情報を定義。
7. 1人利用と夫婦共有の範囲を整理。
8. PDFと見積比較を初期開発へ移動し、主要価値を早く検証。
9. ファイル認可・永続ジョブ・原文を含まないログを要件化。
10. 機能完成と実用性の検証を分け、確認負担を計測対象に追加。
