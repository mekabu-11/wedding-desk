# 検証記録

## 2026-09-12：ご祝儀フォールバック、食事セット、卓レイアウト

- ご祝儀区分に世帯未設定時の適用属性（続柄・側・年齢区分・その他すべて）を追加した。出席中かつ世帯未設定のゲストへ `source_kind: cash_gift_guest` の概算収入を1人1件で作成し、世帯の明示設定を優先する。属性変更・区分変更・世帯削除で未入出金の概算だけを除外／復帰し、確定済み・入出金履歴ありの費用は変更せず変更履歴へ残す。
- 食事セットを大人・子ども・全員の標準設定として登録できるようにした。ゲストごとの個別セット上書きにも対応し、適用セットごとに `source_kind: meal_set` の概算支出を1件だけ作成する。出欠・年齢・セット価格の変更は概算だけを再計算し、確定額と入出金履歴は保持する。
- 席次一覧は従来の卓・メンバー管理を維持し、必要な場合だけ「卓の位置を編集」から卓カードをドラッグ・タッチ・キーボードで配置できるようにした。位置は0〜100の範囲で保存し、個人の卓割当や席順には影響しない。別Weddingの卓IDは更新できない。
- Docker内の `bin/verify`：**120 runs / 971 assertions / 0 failures / 0 errors / 0 skips**。`zeitwerk:check`、`git diff --check`、開発DBへの最新migration適用も成功した。
- 画面確認：ゲストの食事タブ、食事セット追加フォーム、ご祝儀フォールバック設定フォーム、卓レイアウトの空状態をブラウザで確認した。卓が存在する配置保存は統合テストで確認した。

### UI・操作フロー監査

- 致命的な画面崩れは確認しなかった。食事タブは既存のゲスト管理へ追加され、食事セット追加・個人上書き・費用確認の導線がつながっている。
- スマホではハンバーガーを閉じた状態でナビを隠し、開くと縦方向のメニュー、背景タップ、Esc、リンク移動で閉じる実装を確認した。ゲストの5タブは画面幅が狭いと横スクロールになるため、初めて使う人には「食事」が隠れて見える可能性がある。ラベルや導線の見直し候補として残す。
- ご祝儀のフォールバック設定とゲストの食事上書きは詳細折りたたみ内に置いた。通常利用の画面を詰まらせない反面、設定を探す利用者には見落としやすいため、初回だけ説明を表示する改善余地がある。
- 卓レイアウトは卓が未登録の場合の空状態を確認した。実データを変更しないため、複数卓を置いたドラッグの見た目は統合テストとコード確認で担保している。実卓を登録した検証環境では、ポインター・タッチ・矢印キーを実機確認する。
- 食事セット一覧の人数表示は現在の規模では問題ないが、ゲスト数が大きく増えると標準セット判定のクエリが増えるため、将来は集計をまとめる。

## 2026-09-12：検討・決定の関係表示、候補費用履歴、個人引き出物

- 検討・決定画面に、採用候補・関係するやること・関係するお金をまとめて表示し、同じ画面から新規作成できるようにした。候補、費用、やることは任意リンクとして独立している。
- 候補から自動作成する費用は `source_kind: planning_option` と候補IDを保存する。候補変更時は旧費用を削除せず、入出金履歴のない概算だけを除外し、確定済み・入出金履歴あり・他の検討項目でも使う費用は保持する。
- 世帯単位の引き出物とは別に、個人ごとの引き出物セットを割り当て、個人単位の費用を1件だけ作成するようにした。セット内容を変更すると概算だけ再計算し、確定額は維持する。
- スマホの後段CSSでハンバーガーを隠していた上書きを修正し、閉じた状態ではナビを隠し、開いた状態で縦に表示するようにした。
- Docker内の `bin/verify`：**113 runs / 922 assertions / 0 failures / 0 errors / 0 skips**。これはこの時点の記録であり、最新結果は先頭の記録を参照。
- ホストRuby（2.6.10）はGemfileのRuby 3.4指定と不一致のため、Docker外のRailsテストは実行していない。検証はプロジェクトのDocker環境で実施した。

この時点ではご祝儀の属性別フォールバックと食事セットを未実装として記録していた。現在は先頭の記録のとおり実装済みである。

## 2026-09-11：ログイン画面、スマホメニュー、領域横断の変更履歴

- ログイン画面を結婚式の準備をふたりで進めるイラストに変更し、PCでは左右分割、スマホでは上部ビジュアルとフォームの1列構成にした。画像は `app/assets/images/login-wedding-illustration.png` に保存している。
- スマホのヘッダーにハンバーガーメニューを追加した。背景タップ、Escキー、メニュー内リンクで閉じ、44pxのタップ領域を確保する。
- タスク、資料、ゲスト、世帯、席次、お金、引き出物、検討・決定、BGMの手動作成・更新・削除・関連付けを `ChangeEvent` に記録し、ダッシュボードで日本語の対象名・操作名・変更内容を表示する。
- 費用と検討項目の自動作成費用を扱うルールはこの時点では変更していなかった。現在は `source_kind: planning_option` と候補IDを保存し、候補変更時の概算除外と履歴記録を実装済みである。

- 構文確認、Docker内の全テスト **109 runs / 891 assertions / 0 failures / 0 errors / 0 skips**、`zeitwerk:check`、`git diff --check`、development `assets:precompile` が成功した。

## 2026-09-11：手動回帰ハーネス、ガント並び順、最近の変更画面

- `bin/verify` を追加し、Docker内でテストDB準備、全Railsテスト、`zeitwerk:check`、差分チェックを手動実行できるようにした。
- ガントを期限超過、今日進行中、7日以内、それ以降、日付なしの順に並べ、同条件の期限・進捗・開始日・登録順を固定した。統合テストで順序と日付なしの分離を確認した。
- 最近の変更を作成日時とIDの降順で表示し、グループ概要、常時表示の日時、折りたたみ式の個別変更、内部スクロール、全体の最大高さ、領域別アイコン、スマホ1列構成を追加した。
- Excel移行の入口をタスク画面から結婚式設定へ移し、世帯コードを一覧・編集フォームから非表示にした。世帯コードは内部照合とCSVエクスポートに残ることを確認した。
- 月ガントを月初・月末を含む週まで広げ、月またぎタスク、月境界、前後月への移動を統合テストで確認した。結婚式設定画面はセクション間とフォーム内の余白を広げ、設定セクションの構造を固定した。
- ログイン画面に結婚式の準備をふたりで進めるイラストを追加し、PCでは左右分割、スマホでは上部ビジュアル＋フォームの1列構成にした。画像の代替テキストとログイン入力の回帰テストを追加した。
- タスク一覧の余白とカード感を抑え、他の一覧と同じページスクロールの区切り線中心のコンパクトな行にした。一括変更は専用メニューへまとめ、同じ一覧スタイルを資料・ゲスト・お金・検討・決定・ダッシュボードへ適用した。PC幅は最大1280px、スマホ幅は一覧の横はみ出しを抑える構成にした。
- ゲスト・お金・検討・決定・資料に状態や方向別の絞り込みを追加し、状態表示は背景付きのチップを使わず、色付きのドットと文字で一覧性を保った。ゲスト編集の役割選択は詰まりすぎないチェックリストに整理した。お金画面の説明文は内部モデル名を出さず、費用と入出金の状態を確認する文言にした。
- 資料一覧の分類軸を入力形式（文章・写真・スキャン・PDF・ファイル・その他）に統一し、メール・LINE・打ち合わせは情報元の絞り込みへ分離した。既存の `source_type` と旧 `source=` URLは互換性を保ったまま利用できる。
- ガントの月表示は月またぎのタスクを日付列に沿って連続表示し、現在の月を中央に置いた左右矢印、今日への移動、週/月切り替え、バーのドラッグによる開始日・期限の変更、クリック編集を用意した。ブラウザで日付位置と横幅、次月移動を確認した。
- ガントの固定タスク列と日付スクロール領域を別の積層コンテキストに分け、横スクロール時もタスク名がバーの前面に残ることをブラウザで確認した。
- 関連画面の統合テストを追加・更新した。最終的なテスト件数と検証コマンドの結果は、この記録の先頭に追記する。

## 2026-09-09：Phase 4B（タスク管理、ホーム、エクスポート、画面統一）

- タスクのWedding内絞り込み、暗号化文字検索、週／月ガント、期限のみマーカー、絞り込み結果／現在ページの一括変更を追加。2,000件を超える文字検索は切り捨てず、条件追加を求める。
- 一括変更はIDとlock_versionを確認し、別WeddingのIDを拒否。1件でも競合すれば更新とChangeEventを全件ロールバックする統合テストを追加。
- ホームへ今週、期限超過、出席／未回答、検討中、金額未確認、見込収支、未収未払、最近の変更を追加。
- 13種類のUTF-8 BOM付きCSVをZIPで出力。世帯・ゲスト・席次・引き出物セット／品目／割当・収支／入出金・検討項目／候補／BGM・資料メタデータの関係IDを保持し、添付原本は含めない。表計算ソフトの数式として解釈される先頭文字を無害化する。
- Dockerのweb/workerを同一イメージに統一し、両方の起動を確認。全テスト **91 tests / 650 assertions / 0 failures / 0 errors**、`zeitwerk:check`、`git diff --check`成功。
- 一時QAアカウントと架空データでデスクトップのダッシュボード・タスク・ガント、390px幅のダッシュボード・タスク・横スクロール式ガントを目視確認。CSPを維持したまま動的ガント位置をCSSクラス化し、確認用データは削除した。

## 2026-09-08：段階3（画像・PDF添付、出典、横断候補）

- 架空の1px PNGで本文なし資料を保存し、Active Storage添付、Wedding外からのIDOR拒否、認証済みダウンロードの`private, no-store`を確認。
- 空資料、SVG、壊れた画像・PDF、1ファイル上限、10個上限を統合テストで確認。最大値テストは定数を一時的に下げて実データを小さく保ち、実容量を作らない。
- SourceLinkのWedding整合性、資料に属さない添付の拒否、資料削除後のTask保持と削除ChangeEventを確認。既存Taskの出典リンクはmigrationでbackfillする。
- ChangeOperationの型別属性allowlist、未知属性、作成キー依存、根拠引用、添付所属、別Wedding対象、lock_version競合、依存不足、全体rollback、同じChangeSetの再適用を確認。
- Dockerの空テストDBへ全migrationを初回適用し、段階3対象テスト（13 tests / 87 assertions）、全テスト（80 tests / 553 assertions）、`zeitwerk:check`、`git diff --check`を実行し、すべて成功。

外部AIのResponses APIへの実送信はこの検証では行わない。現行のCrossDocumentAiアダプターは未設定時に送信せず失敗状態へ遷移する安全な境界であり、実プロバイダー接続、画像・PDFの外部送信、HEICの成功変換は未検証である。Dockerイメージにはlibvips・libheif・popplerの実行ファイルを含めるが、実在資料での解析精度は保証しない。

## 2026-09-08：段階2（検討・決定、BGM、関連付け、履歴）

- 検討項目・候補・採用／見送り、BGM詳細、費用リンク、タスクリンク、別Wedding拒否を架空データの統合テストで確認。
- 採用候補は1項目1件、採用時の費用選択はトランザクション内で処理し、既存BudgetItemの再利用と3項目から同一BudgetItemへのリンクでも集計を増幅しないことを確認。
- Guest出欠変更、BudgetEstimateRecalculator、採用操作のChangeEventとactor/source、担当の旧値移行互換を確認。
- Dockerで空のテストDBへ全migrationを初回適用し、段階2対象テスト（8 tests / 83 assertions）、全テスト（67 tests / 468 assertions）、`zeitwerk:check`、`git diff --check`を実行し、すべて成功。

## 2026-09-08：段階1（ゲスト・世帯・席次・引き出物・収支基盤）

- 架空データの統合テストを追加：ゲストCRUD、別Weddingのゲスト／BudgetItem IDOR拒否、世帯ごとのご祝儀BudgetItem一意性、出欠・属性変更による概算数量の再計算と確定額固定、区分・引き出物内訳変更による概算再計算、内金・返金・超過表示、引き出物割当の単一BudgetItem、GiftSet内訳の税抜単価計算、属性・検索・二重送信・タブ別表示分離を確認。
- Ruby各モデル・コントローラ・migration・テストの構文確認と`git diff --check`は成功。
- DockerでテストDBを削除・再作成し、全migrationを初回適用したうえで`db:prepare`相当、段階1対象テスト（20 tests / 138 assertions）、全テスト（67 tests / 468 assertions）、`zeitwerk:check`を実行し、すべて成功。`git diff --check`も成功。
- 画面はERBのゲスト4タブ、世帯／卓／ご祝儀区分／引き出物セット／割当、BudgetItem／MoneyMovementの手動CRUDを追加済み。統合テストで各一覧・入力・編集画面のHTMLレンダリングを確認した。Macがロック中のため、CUAブラウザによる実機画面確認は未実施。

## 2026-09-07：2アカウント共有・資料削除後のタスク保持

- Membershipへ既存所有者を移行し、開発DBでusers 1 / weddings 1 / memberships 1、所属のないWedding 0を確認。
- 管理者が設定画面から2人目を追加し、2人目のログインから同じタスクを閲覧できる統合テストを追加。
- メンバーによる3人目追加を拒否し、Weddingを最大2人に制限するテストを追加。
- 資料削除後も承認済みタスクを保持し、Candidate参照を外して削除済み出典を表示するテストを追加。
- Dockerで全39テスト・247 assertions成功。`bin/rails zeitwerk:check`成功。
- 実際の2人目アカウント登録とスマホ実機操作は未実施。

## タスク準備管理の追加検証（2026-09-06）

- Docker内の全テスト：37 tests / 224 assertions / 0 failures / 0 errors。
- 手動追加・出典なしの編集、JSONプレビューと確認後の登録、所有者分離、暗号化、同じ出典の二重登録防止、手修正維持、失敗時の全件ロールバックを検証。
- 開始日・今週への期間重複、日本時間の深夜境界、担当絞り込みと期限超過を検証。
- 実際の45行をDBへ書き込まずImportTasksで検証。
- ブラウザで一時アカウントを使い390px幅の手動追加と1280px幅のプレビュー・登録を確認。横はみ出しなし。確認用データは削除済み。
- Railsのzeitwerk:check成功。利用者の45件の本登録とスマホ実機検証は未実施。

## Phase 4A：xlsx全体移行

- 架空xlsxをRubyZip/Nokogiriで生成する対象テストを追加し、テンプレート識別、派生シート除外、数式非実行、同一digest再取込防止、zip slip/壊れたxlsx拒否、行単位警告、Weddingスコープ、transaction rollback、世帯・引出物・BGM・金額未確認を検証する。
- 反映順はマスタ→世帯→ゲスト→引出物→収支→タスク→BGM。実績額・日付不明の入出金からconfirmed/MoneyMovementを作らず、Taskのsource_keyは既存JSON取り込みと同じ出典キー式を使う。
- 実際の2ブックは読み取り専用dry-runで構造・件数・警告数のみ確認する。氏名・金額・抽出JSONはリポジトリへ保存せず、DB反映もしない。
- 実ファイルのdry-runは合計295行（task 45、bgm 20、budget_item 46、cash_gift_rule 7、gift_assignment 54、gift_set 5、guest 64、household 54）、行単位警告225件（警告メッセージ種別6）として確認した。表記揺れ修正後、cached単価候補を持つギフトセットは5件、transaction rollback内の確認済み反映で割当候補54件を確認した。出力はこの件数だけに限定し、永続反映は行っていない。

以下は当初MVPの検証記録。

検証日：2026-09-06。ローカルのDocker Desktop / Compose v2.40.3、ARM64。

## 自動テスト

`bin/ci` 成功：**30 tests / 140 assertions / 0 failures / 0 errors / 0 skips**。

確認した項目：

- ログイン、初期設定、登録、承認、タスク編集、削除の一連のフロー
- 他所有者の資料・候補・タスクに対する閲覧・変更・削除・再解析の拒否
- CSRFトークンのない更新リクエストの拒否
- 原文のHTMLエスケープ、no-storeレスポンス
- 原文・候補payloadのDB内暗号化
- 原文の完全一致重複、再解析・再実行による二重生成の防止
- 2スレッドでの同時承認と同時解析開始
- タスクの手修正維持、楽観ロックによる競合検出
- JSON Schema・原文引用・不正な日付・存在しない日付を含む日時の検証
- 日本時間の日付期限の境界
- APIキー未設定、拒否、途中終了、HTTPエラー、最大3回のリトライ
- 有効な候補0件と解析失敗の区別
- 削除済み資料をジョブが復活させないこと

`bin/rails zeitwerk:check` 成功。

## Docker

- `bin/setup` で初回の秘密値生成・イメージ作成・DB初期化・アカウント作成・起動を確認。
- 最終コードで `docker compose build` 成功（productionのassets:precompileを含む）。
- Web・PostgreSQL・Solid Queueワーカーの3サービスを起動。
- ワーカーを停止してサンプルの再解析を投入し、pendingのRunと永続キュー内のJobを確認。ワーカー再起動後にcompletedとなり、候補2件・既存タスク1件を維持。
- `docker compose -f compose.yaml -f compose.production.yaml config --quiet` 成功。
- ActiveRecordのSQLロガーが無効であることをランタイムで確認。

## ブラウザ

一時的なローカル検証アカウントと架空の文章のみで確認。

- ログイン → 結婚式設定 → ダッシュボード
- サンプル登録 → 実ワーカーの処理 → 根拠付き候補表示
- 候補承認 → タスク一覧への反映 → 状態を進行中へ変更
- 一般の架空メモを登録し、API未設定の案内と原文保存を確認
- デスクトップのダッシュボードを画像で確認
- 390px幅でダッシュボード・候補確認・登録・タスク一覧を確認。ページ幅390px、フォーム部品の横はみ出しなし
- モバイルでも結婚式設定からログアウト可能

検証用アカウント・資料は確認後に削除。初期所有者アカウントは残し、利用者が結婚式設定から開始する。

## 未検証・対象外

- 実際のOpenAI APIへの通信、モデルの利用権限・費用・抽出精度（APIキー未設定）。リクエスト形式とエラー処理はWebMockで検証。
- 実在の結婚式資料20件での精度・確認時間評価
- PDF・画像・見積比較・回答待ち・決定事項（次段階）
- 実環境への公開、TLSプロキシ、バックアップ復元、公開運用の耐負荷検証

今回の完成範囲は「テキスト登録から候補確認・タスク管理まで」。全体MVPの完成とは区別する。
